<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="post">
	<select id="PostList" parameterType="string" resultType="PostVO">
		select tp.post_number, post_title, tu.user_nickname, tpf.post_file_system_name
		from tbl_post tp
		left outer join tbl_user tu on tp.user_number = tu.user_number
		left outer join tbl_post_file tpf on tpf.post_number = tp.post_number
	</select>

</mapper>









