<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	<insert id="join" parameterType="UserDTO">

		insert into tbl_user
		(user_id,
		user_password, user_name, user_nickname, user_phone_number,
		user_email, user_address, user_gender)
		values(#{userId},
		#{userPassword}, #{userName}, #{userNickName},
		#{userPhoneNumber},
		#{userEmail}, #{userAddress},
		#{userGender})
	</insert>

	<select id="checkId" parameterType="string" resultType="_int">
		select
		count(user_number) from tbl_user tu
		where user_id = #{userId}
	</select>


	<select id="login" parameterType="UserDTO" resultType="_int">
		select
		user_number from tbl_user
		where user_id = #{userId} and user_password =
		#{userPassword}
	</select>

	<select id="getUserId" parameterType="_int" resultType="String">
		select
		user_id from tbl_user
		where user_number = #{userNumber}
	</select>

	<select id="findPassword" parameterType="UserDTO"
		resultType="String">
		select user_password from tbl_user
		where user_phone_number =
		#{userPhoneNumber} and user_email = #{userEmail}
	</select>

	<select id="selectMemberProfileInfo" parameterType="ProfileEditVO" resultType="_int">
		select u.user_id,
		u.user_password, u.user_nickname, u.user_phone_number, u.user_name,
		u.user_phone_number,
		u.user_email, u.user_name,
		u.user_self_introduction,
		p.user_profile_image_system_name,
		p.user_profile_original_name
		from tbl_user u join tbl_user_profile_image p
		on user_number = #{userNumber} and u.user_number = p.user_number;
	</select>
	
</mapper>



















